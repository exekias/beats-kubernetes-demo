apiVersion: v1
kind: Service
metadata:
  name: beats-demo
spec:
  type: LoadBalancer
  # Avoid NAT to get source IP:
  externalTrafficPolicy: Local
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: beats-demo
